---
# Source: deployment/templates/auth-config.yaml
# Secret for auth service credentials
apiVersion: v1
kind: Secret
metadata:
  name: auth-service-secrets
  namespace: app
  labels:
    helm.sh/chart: deployment-0.1.0
    app.kubernetes.io/name: auth-service
    app.kubernetes.io/instance: app-deployment
    app.kubernetes.io/component: auth
    app.kubernetes.io/version: "1.0"
    app.kubernetes.io/managed-by: Helm
type: Opaque
stringData:
  db_user: "admin"
  db_password: "password"
  db_host: "localhost"
  db_port: "5432"
  db_name: "auth_db"
---
# Source: deployment/templates/auth-config.yaml
# ConfigMap for auth service configuration
apiVersion: v1
kind: ConfigMap
metadata:
  name: auth-service-config
  namespace: app
  labels:
    helm.sh/chart: deployment-0.1.0
    app.kubernetes.io/name: auth-service
    app.kubernetes.io/instance: app-deployment
    app.kubernetes.io/component: auth
    app.kubernetes.io/version: "1.0"
    app.kubernetes.io/managed-by: Helm
data:
  virtual_server_enabled: "true"
