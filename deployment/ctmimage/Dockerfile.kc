FROM quay.io/keycloak/keycloak:26.4.7
# Build optimized image with PostgreSQL driver and runtime configuration
RUN /opt/keycloak/bin/kc.sh build \
    --db=postgres \
    --http-relative-path=/ \
    --health-enabled=true \
    --metrics-enabled=true